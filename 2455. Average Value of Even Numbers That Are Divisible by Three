class Solution:
    def averageValue(self, nums: List[int]) -> int:
        count = 0
        summ = 0
        for i in nums:
            if i % 6 == 0:
                count += 1
                summ += i

        return summ // count if count else 0
        
